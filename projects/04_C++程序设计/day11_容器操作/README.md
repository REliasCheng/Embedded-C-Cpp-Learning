# vector 容器的增删改查（day11）

## 内容介绍

这个课堂例子围绕 `std::vector<int>` 完成追加、修改、遍历和删除，作为后续理解容器接口与边界的起点。

## 核心知识

`push_back` 在尾部追加；`at` 访问时进行边界检查；`operator[]` 不做同样的检查；`erase(scores.begin())` 删除首元素并使后续位置移动。

## 代码结构

[课程原版](course/day02/05_vector/02_main.cpp)来自 `day11-c++/上课代码.rar` 内的 `day02/05_vector/02_main.cpp`。全部操作集中在 `main`，没有自定义容器或算法模块。

## 关键实现

原例先把索引 0、1 改为 55、56，再遍历输出；删除首元素后再次遍历。两轮循环用 `int i` 比较 `scores.size()`，本机编译器因此报告有符号/无符号比较警告。

## 工程意义

我在这个例子中看到容器操作不只是语法：访问方式决定边界保护，删除操作会影响索引和迭代器。未来若把容器用于资源受限环境，还需评估动态分配成本。

## 调试记录

本次使用 G++ 16.1.0 和 GBK 输入编码编译运行通过，退出码 0；保留两处 `-Wsign-compare` 警告，未修改课程循环。没有运行同课程中故意越界的演示文件。
