# C 语言基础能力

## 项目简介

这一组代码建立程序入口、数据表示、控制流和连续内存访问的基础。课程阶段编号只保存在各主题 `docs/来源-day*.md`；这里按技术问题查找。

## 技术背景

嵌入式接口经常先遇到“值占多少位”“缓冲区有多长”“输入走哪个分支”，这些问题比语法顺序更直接。

## 软件结构

| 主题 | 课程代码 | 个人实践 |
| --- | --- | --- |
| [程序入口与控制流](程序入口与控制流/docs/来源-day01.md) | `main`、标准输出、`switch` | 暂无独立版本 |
| [数据类型与内存模型](数据类型与内存模型/docs/来源-day02.md) | 定宽整数、按位与 | 暂无独立版本 |
| [数组与字符串](数组与字符串/docs/来源-day06.md) | 数组传参、字符数组与指针 | [显式长度的数组求和](数组与字符串/practice/array_sum.c) |

## 核心实现

课程的数组函数只接收首元素地址，循环次数固定为 5。个人版本增加 `count` 参数并对空指针和求和溢出返回失败，源码单独置于 `practice/`，不覆盖课程原版。

## 关键代码分析

`sizeof values / sizeof values[0]` 在调用点得到当前数组长度；函数内部只相信显式传入的 `count`。字符数组例子则区分修改数组内容与重新指定指针地址。[课程边界记录](数组与字符串/docs/来源-day07.md)保留原练习语境。

## 调试记录

课程精选例子已分别在本机 GCC 16.1.0 编译运行。`array_sum.c` 以 `-Wall -Wextra -Werror -pedantic` 构建并通过非空、空数组和无效指针测试；本机验证不代表所有目标平台。

## 嵌入式关联

定宽类型和位掩码是寄存器/协议字段的语言基础；显式缓冲区长度有助于限制越界风险。本组仍是主机端练习，没有访问真实 MCU 寄存器。

## 展示区域

当前以源码、测试输出和分析为主。真实终端截图或后续硬件关联图可放入 [assets](../../assets/README.md)，不预置假图片。
