# 来源说明

`projects/` 中各 `course/` 目录的源码选自“嵌入式 C/C++ 程序设计”课程配套资料，包括 day01–day09 的 C 文件和 day10–day13 的 C++ 文件。文件内容按原始字节迁入；各主题 `docs/来源-dayXX.md` 记录原始相对路径。

主题索引、技术分析和 `practice/` 下的边界检查、多文件接口、封装及状态机示例由仓库维护者编写。

根目录 `LICENSE` 适用于仓库维护者编写的代码和文档，包括 `practice/` 下的个人实现。`course/` 中的源码继续适用其原始许可条件。

个人实现位于 `practice/`，课程示例位于 `course/`。
