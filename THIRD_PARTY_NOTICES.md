# 来源说明

`projects/` 中各 `course/` 目录的源码选自“嵌入式 C/C++ 程序设计”课程配套资料，用作学习实践和代码分析。包括从 day01–day09 直接选取的 C 文件，以及从 day10–day13 课程压缩包中选取的 C++ 文件。文件内容按原始字节迁入，没有改写函数、注释或教学示例中的问题。各主题 `docs/来源-dayXX.md` 记录原始相对路径；V1 三个专题的分析文档也继续保留。

主题索引、技术分析和 `practice/` 下的边界检查、多文件接口、封装及状态机示例为个人编写。课程源码的归属不因收录而改变，根目录许可证不为课程源码重新授权；复用前请核对原提供方的许可条件。

根目录 `LICENSE` 只覆盖仓库作者有权授权的原创代码和文档，包括 `practice/` 下的个人实现；它不改变 `course/` 中课程源码的原始许可。

仓库不收录课程视频、讲义整包、安装程序或编译产物。独立实现放在 `practice/`，不覆盖 `course/`。
